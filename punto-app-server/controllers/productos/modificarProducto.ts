import { Request, Response, NextFunction } from "express";
const modificarProductosHandler = (req: Request, res: Response, {/*next: NextFunction*/}) => {
    console.log("Ruta de modificar productos llamada")
};
export default modificarProductosHandler