import { Request, Response, NextFunction } from "express";
const borrarCategoriaHandler = (req: Request, res: Response, {/*next: NextFunction*/}) => {
    console.log("Ruta de borrar categorias llamada")
};
export default borrarCategoriaHandler