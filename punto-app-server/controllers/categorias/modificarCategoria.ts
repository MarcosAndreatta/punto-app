import { Request, Response, NextFunction } from "express";
const modificarCategoriaHandler = (req: Request, res: Response, {/*next: NextFunction*/}) => {
    console.log("Ruta de modificar categorias llamada")
};
export default modificarCategoriaHandler